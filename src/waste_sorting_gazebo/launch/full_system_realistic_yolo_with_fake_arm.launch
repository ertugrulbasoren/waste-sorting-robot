<launch>
  <!--
    Wrapper launch:
      Full realistic YOLO sorting pipeline
      +
      Fake visual robot arm

    This launch does not change the working perception/sorting pipeline.
    It only adds a visual robot arm to the Gazebo scene.
  -->

  <!-- Existing realistic YOLO pipeline args -->
  <arg name="gui" default="true"/>
  <arg name="paused" default="false"/>
  <arg name="use_sim_time" default="true"/>

  <arg name="conf_threshold" default="0.25"/>
  <arg name="spawn_interval_sec" default="7.0"/>

  <!-- This is the correct current height arg used by full_system_realistic_yolo.launch -->
  <arg name="object_bottom_z" default="0.145"/>
  <arg name="visual_lift" default="0.0"/>

  <arg name="conveyor_speed" default="0.10"/>
  <arg name="end_x" default="0.20"/>

  <!-- Fake arm pose args -->
  <arg name="arm_model_name" default="fake_sorting_arm"/>
  <arg name="arm_x" default="0.55"/>
  <arg name="arm_y" default="0.85"/>
  <arg name="arm_z" default="0.00"/>
  <arg name="arm_yaw" default="-1.5708"/>

  <!-- Start the already working realistic YOLO full pipeline -->
  <include file="$(find waste_sorting_gazebo)/launch/full_system_realistic_yolo.launch">
    <arg name="gui" value="$(arg gui)"/>
    <arg name="paused" value="$(arg paused)"/>
    <arg name="use_sim_time" value="$(arg use_sim_time)"/>

    <arg name="conf_threshold" value="$(arg conf_threshold)"/>
    <arg name="spawn_interval_sec" value="$(arg spawn_interval_sec)"/>

    <arg name="object_bottom_z" value="$(arg object_bottom_z)"/>
    <arg name="visual_lift" value="$(arg visual_lift)"/>

    <arg name="conveyor_speed" value="$(arg conveyor_speed)"/>
    <arg name="end_x" value="$(arg end_x)"/>
  </include>

  <!-- Spawn fake visual robot arm once -->
  <node
    pkg="waste_sorting_gazebo"
    type="spawn_fake_robot_arm_visual.py"
    name="spawn_fake_robot_arm_visual"
    output="screen"
    respawn="false">

    <param name="model_name" value="$(arg arm_model_name)"/>
    <param name="x" value="$(arg arm_x)"/>
    <param name="y" value="$(arg arm_y)"/>
    <param name="z" value="$(arg arm_z)"/>
    <param name="yaw" value="$(arg arm_yaw)"/>
    <param name="delete_existing" value="true"/>
  </node>
</launch>
